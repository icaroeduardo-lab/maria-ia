# Segredos — NUNCA na imagem
.env
.env.*

# Deps e build (reinstalados/gerados no build)
node_modules
dist

# Estado local efêmero
data
*.db
*.sqlite

# Git e metadados
.git
.gitignore
.github

# Não usados no build do backend (docs entra: openapi.yaml servido em /docs)
docs/*
!docs/openapi.yaml
cloudformation
test
.claude
*.log
README.md
